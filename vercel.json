{
  "version": 2,
  "buildCommand": "cd my-app && npm install && npm run build",
  "outputDirectory": "my-app/build",
  "installCommand": "cd server && npm install && cd ../my-app && npm install",
  "functions": {
    "api/index.js": {
      "includeFiles": "server/**"
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
